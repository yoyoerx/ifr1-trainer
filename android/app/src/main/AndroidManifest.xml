<?xml version="1.0" encoding="utf-8"?>
<!--
UNVERIFIED SCAFFOLD - see android/README.md. USB-host support (needed for
the Octavi IFR-1, §3.1 of docs/ANDROID_PORT_PLAN.md) is deliberately not
declared yet: uses-feature android.hardware.usb.host and the
UsbManager/attached-device intent-filter belong here once the Phase 0
hardware spike has actually run and its outcome is known, not guessed at
in advance.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.OctaviIfrTrainer">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape">
            <!-- landscape hardcoded for Phase 1 (§6) - portrait is its own
                 later phase (§6 Phase 2 / §3.3), not toggled here yet -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
