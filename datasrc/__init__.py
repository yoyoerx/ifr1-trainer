"""Nav-data sourcing: AIRAC cycle math and FAA product fetching.

This package is used only by the offline `datasrc` CLI and by `navdata.py` at
load time. Nothing here runs inside the trainer's main loop.
"""
